#include "Struct.h"



tree *newtree(int x)

{

    tree *tr = malloc(sizeof(*tr));

    if(tr == Null){

        fprintf("L'allocation de l'arbre ne c'est pas faite.\n");

        exit(EXIT_FAILURE);

    
    }


    free(tree);
}

